// const fs = require('fs');
class Screen {
    constructor() {
        this.testFunction();
        // this.buttons = args.buttons;
        // this.createButtons(args.listOfButtons);
    }

    testFunction() {
        // const fs = require('fs');
        // let rawData = fs.readFileSync('config.json');
        // let buttons = JSON.parse(rawData);
        // debugger

    }

    createButtons(listOfButtons) {
        // this.listOfButtons = listOfButtons;
        // listOfButtons.forEach(button => {
        //     this.createButton(button);
        // });
    }

    createButton(button) {

    }
}